extends layouts/main.pug

block styles
  link(rel="stylesheet", href="/css/forms.css")

block content
  form.users-form(action="/users", method="POST")
    .form-control
      label(for="username") UserName
      input(type="text", name="user", placeholder="john_doe_xyz123")#user
    button.btn(type="submit") Add UserName
